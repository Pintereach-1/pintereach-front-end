const initialState = {
    articles: [],
    boards: [],
    user_info: {},
    error: '',
    isLoading: false,
}

export const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
        return state;
    }
  };